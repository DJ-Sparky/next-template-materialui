import { Button } from '@mui/material';
import type { NextPage } from 'next';

const Home: NextPage = () => {
  return (
    <>
      <h1>NextJS Template</h1>
      <Button variant="contained">With Material-UI</Button>
    </>
  );
};

export default Home;
